{
  "name": "pfe-dev-launcher",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install && npm run setup:backend && npm run setup:frontend",
    "setup:backend": "python3 -m venv backend/.venv && backend/.venv/bin/pip install -r backend/requirements.txt",
    "setup:frontend": "cd frontend && npm install",
    "backend": "backend/.venv/bin/python backend/manage.py runserver 8001",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently -n BACK,FRONT -c blue,green \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
